# Firmware tests

Stage 0 keeps hardware behavior behind future module interfaces and validates the end-to-end state machine in `simulator/`. After the device arrives, add PlatformIO Unity tests for WAV header generation, queue recovery and button-duration classification here.
